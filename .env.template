# Supabase Configuration
SUPABASE_URL=https://xxxx.supabase.co
DATABASE_URL=
SUPABASE_ANON_KEY=
STORAGE_BACKEND=supabase
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_STORAGE_BUCKET=

# Optional: Flask Secret Key
SECRET_KEY=generate_a_random_secret_key_here

# Jina embeddings v3 (fallback to keyword matching if left empty)
# Get API key at https://jina.ai/embeddings/
JINA_API_KEY=jina_xxxx

# SMTP Configuration (if still needed for other purposes, though Supabase handles Auth emails)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_SENDER=